apiVersion: v1
kind: Service
metadata:
  name: api-service
  labels:
    app: futurapredict
spec:
  type: ClusterIP
  selector:
    app: futurapredict
    component: api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
